# Pathfinder Integration Test Configuration
# Copy this file to .env and fill in your values

# EVE Online SSO Application Credentials
# Get these from: https://developers.eveonline.com/applications
ESI_CLIENT_ID=your_client_id_here
ESI_CLIENT_SECRET=your_client_secret_here

# Optional: Refresh Token for authenticated endpoints
# Most integration tests use public endpoints and don't require this
# To get a refresh token:
# 1. Create an SSO application at https://developers.eveonline.com/applications
# 2. Set callback URL to http://localhost/callback (or your preferred callback)
# 3. Add required scopes if needed
# 4. Use the SSO login flow to authenticate
# 5. Exchange the authorization code for tokens
# 6. Store the refresh_token value here
ESI_REFRESH_TOKEN=your_refresh_token_here

# Optional: Test Character ID
# The character ID that the refresh token belongs to (if using authenticated endpoints)
ESI_CHARACTER_ID=your_character_id_here

# Optional: ESI Server Configuration
# ESI_BASE_URL=https://esi.evetech.net
# ESI_DATASOURCE=tranquility

# Optional: SSO Server
# SSO_BASE_URL=https://login.eveonline.com
